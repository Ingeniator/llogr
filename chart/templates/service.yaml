apiVersion: v1
kind: Service
metadata:
  name: {{ include "llogr.fullname" . }}
  labels:
    {{- include "llogr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llogr.selectorLabels" . | nindent 4 }}
